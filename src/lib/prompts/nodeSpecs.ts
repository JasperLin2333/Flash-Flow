export const NODE_SPECS = `
# 📦 节点参数详解 (Strict Code-Grounding)

## 1. Input 节点

### 1.0 参数表
| 参数 | 类型 | 默认值 | 取值范围/说明 |
|------|------|-------|-------------|
| \`enableTextInput\` | boolean | \`true\` | 启用文本输入框 |
| \`enableFileInput\` | boolean | \`false\` | 启用文件上传 |
| \`enableStructuredForm\` | boolean | \`false\` | 启用结构化表单：预置配置参数（选项/数值），运行时自动弹窗采集，供下游分支判断或 LLM 引用 |
| \`greeting\` | string | \`"我是您的智能助手，请告诉我您的需求。"\` | 招呼语，引导用户如何使用该助手 |
| \`fileConfig.allowedTypes\` | string[] | \`["*/*"]\` | 允许的文件类型 |
| \`fileConfig.maxSizeMB\` | number | \`100\` | 单文件最大体积 (MB) |
| \`fileConfig.maxCount\` | number | \`10\` | 最大文件数量 |

> 🔴 **输入配置铁律**
> - 涉及 **文件/图片/文档** → \`enableFileInput: true\` + \`fileConfig.allowedTypes\`
> - 涉及 **可选模式/风格/策略等预设选项** → \`enableStructuredForm: true\` + \`formFields\`
>   - 典型场景：分析模式(基本面/技术面)、风险偏好(保守/激进)、输出风格(简洁/详细)、语言选择
> - **greeting** 招呼语：根据应用场景，用 1-2 句话引导用户如何使用该助手，例如：
>   - 翻译助手 → "请输入需要翻译的文本，我会帮您翻译成目标语言"
>   - 文档分析 → "请上传您的文档，我将帮您提取关键信息并进行分析"
>   - 智能客服 → "有任何问题都可以问我，我会尽力为您解答"

### 1.1 allowedTypes 常用值
| 文件类型 | allowedTypes |
|---------|-------------|
| 图片 | \`[".jpg", ".jpeg", ".png", ".webp"]\` |
| 文档 | \`[".pdf", ".docx", ".doc"]\` |
| 表格 | \`[".csv", ".xlsx", ".xls"]\` |


### 1.2 formFields 字段类型
| type | 说明 | 必填属性 | 可选属性 |
|------|------|---------|---------|
| \`text\` | 文本输入框 | \`name\`, \`label\` | \`required\`, \`defaultValue\`, \`placeholder\` |
| \`select\` | 下拉选择 | \`name\`, \`label\`, \`options\`[] | \`required\`, \`defaultValue\` |
| \`number\` | 数字输入 | \`name\`, \`label\` | \`required\`, \`defaultValue\`, \`min\`, \`max\` |

### 1.3 完整配置示例
\`\`\`json
{"id": "input_1", "type": "input", "data": {
  "label": "用户输入",
  "enableTextInput": true,
  "enableFileInput": true,
  "fileConfig": {"allowedTypes": [".pdf", ".docx"], "maxSizeMB": 10, "maxCount": 5},
  "enableStructuredForm": true,
  "formFields": [
    {"type": "text", "name": "stock", "label": "股票代码", "required": true, "placeholder": "如：AAPL"},
    {"type": "select", "name": "mode", "label": "分析模式", "options": ["基本面", "技术面", "综合"], "defaultValue": "综合"}
  ]
}}
\`\`\`

## 2. LLM 节点

### 2.0 参数表
| 参数 | 类型 | 默认值 | 取值范围/说明 |
|------|------|-------|-------------|
| \`model\` | string | \`deepseek-ai/DeepSeek-V3.2\` | 见下方可用模型列表 |
| \`temperature\` | number | \`0.7\` | 0.0-1.0 (低=确定性, 高=创造性) |
| \`systemPrompt\` | string | \`""\` | 系统提示词，支持 \`{{变量}}\` |
| \`enableMemory\` | boolean | \`false\` | 是否启用多轮对话记忆 |
| \`memoryMaxTurns\` | number | \`10\` | 1-20, 最大记忆轮数 |
| \`responseFormat\` | string | \`"text"\` | \`"text"\` 或 \`"json_object"\` (强制JSON) |
| \`inputMappings.user_input\` | string | 可选 | 用户消息来源，如 \`{{用户输入.user_input}}\` |

> 🔴 **JSON 模式铁律**
> - \`responseFormat: "json_object"\` 时，**必须**在 systemPrompt 中明确要求输出 JSON 格式。

> 🔴 **user_input 配置铁律 - 二选一，禁止重复！**
> 
> 用户输入只能通过**一种方式**传递给 LLM，以下两种方式**互斥**：
> 
> | 方式 | 适用场景 | 示例 |
> |------|---------|------|
> | **A. inputMappings.user_input** | 简单对话/问答，用户消息作为独立的 user 角色发送 | \`inputMappings: {user_input: "{{用户输入.user_input}}"}\` |
> | **B. systemPrompt 内引用** | 复杂场景，用户输入需要与其他上下文组合 | \`systemPrompt: "分析 {{输入.user_input}} 结合 {{搜索.results}}..."\` |
> 
> ❌ **严禁同时使用 A+B**: 会导致用户输入被重复发送两次！
> 
> **场景选择指南**:
> - 纯对话/聊天/问答助手 → 使用 **A** (配置 inputMappings.user_input)
> - 多步骤工具链 (systemPrompt 已引用用户输入变量) → 使用 **B** (不配置 inputMappings.user_input)
> - 文件/图片处理 → **必须**先接 RAG 节点，LLM 引用 \`{{RAG.documents}}\`
>
> 🔵 **输出变量 (LLM 节点提供)**:
> - \`{{LLM.response}}\`: 模型生成的回复内容
> - \`{{LLM.reasoning}}\`: 推理模型的思维链 (如 DeepSeek Reasoner)
> - \`{{LLM.token_usage}}\`: 消耗概览字符串 (如 "Prompt: 100, Completion: 50")
> - \`{{LLM.usage.total_tokens}}\`: 总消耗 Token 数 (数值)

### 2.1 可用模型列表 (必须从此列表选择)
| model 值 | 说明 |
|---------|------|
| \`deepseek-ai/DeepSeek-V3.2\` | DeepSeek-V3.2 (SiliconFlow) - 默认 |
| \`deepseek-v3-2-251201\` | DeepSeek-V3.2 (火山引擎) |
| \`deepseek-chat\` | DeepSeek-V3.2 (官方) |
| \`doubao-1-5-pro-32k-character-250715\` | doubao-1.5-pro |
| \`doubao-seed-1-6-flash-250828\` | doubao-seed-1.6-flash |
| \`qwen-flash\` | 千问模型-快速 |

> 🔴 **文件/图片处理不要选视觉模型**: LLM 无法直接读取文件内容，必须先通过 RAG 节点解析

### 2.2 记忆功能配置铁律 🧠

> 🔴 **enableMemory 配置铁律**
> - 客服/对话/聊天/咨询/问答/助手 → \`enableMemory: true\`
> - 翻译/摘要/分类/提取/识别/分析 → \`enableMemory: false\`

### 2.3 SystemPrompt 最佳实践
1. **必须包含业务逻辑**: 明确"你是谁"、"任务目标"、"输出格式"
2. **禁止**仅写 "你是助手"，**禁止**使用数组下标 \`files[0].name\`

## 3. RAG 节点 (Gemini 检索增强)

> 🔴 **RAG 模式配置铁律 - 根据场景选择！**
> - 用户**上传文件**需要分析 → 动态模式：配置 \`inputMappings.files\`
> - 需要检索**预设知识库** → 静态模式：不配置 \`inputMappings.files\` (需在 UI 预上传)
> - **query 必填**: 检索查询内容不能为空！
>
> 🔴 **文件/图片处理铁律 - 必须使用 RAG 节点！**
> - 涉及**文档分析、PDF提取、文件问答** → **必须**使用 RAG 节点处理文件
> - 涉及**图片理解、图片内容提取** → **必须**使用 RAG 节点处理图片
> - ❌ **禁止**直接将 \`{{xx.files}}\` 传给 LLM 节点，LLM 无法直接读取文件内容
> - ✅ 正确流程: Input(上传文件) → RAG(检索/解析) → LLM(基于 \`{{RAG.documents}}\` 分析)

### 3.1 参数限制
| 参数 | 类型 | 默认值 | 取值范围 | 说明 |
|------|------|-------|---------|------|
| \`maxTokensPerChunk\` | number | 200 | 50-500 | 静态分块大小 (tokens) |
| \`maxOverlapTokens\` | number | 20 | 0-100 | 静态分块重叠 (tokens) |

### 3.2 模式配置
| 模式 | 场景 | inputMappings |
|------|------|---------------|
| **动态** | 用户上传文件分析 | \`query: "{{用户输入.user_input}}"\` + \`files: "{{用户输入.files}}"\` |
| **静态** | 固定知识库检索 | \`query: "{{用户输入.user_input}}"\` |

> 💡 **query 配置提示**: \`query\` 通常引用用户的文本输入作为检索关键词

### 3.3 输出变量
- \`{{xx.documents}}\`: 检索到的文档片段列表
- \`{{xx.citations}}\`: 引用来源信息

## 4. Tool 节点
根据 \`registry.ts\` 严格匹配参数：

> ⚠️ **参数类型铁律**:
> 1. **数值型参数** (如 \`maxResults\`, \`maxLength\`): 必须填入**静态数值** (Number)，**禁止**使用 \`{{变量}}\` (引擎不支持 String->Number 自动转换)。
> 2. **复杂对象/数组**: 必须填入静态 JSON，**禁止**内部引用变量 (引擎不递归解析)。

| 工具ID/Type | 说明 | 适用场景 | 必填 inputs | 选填 inputs | 输出变量 (示例) |
|--------|------|---------|-------------|------------|---------|
| \`web_search\` | 关键词搜索 | "搜索XX"、"查找XX信息" | \`query\`(搜索词) | \`maxResults\`(Integer, 1-10, 默认5) | \`{{节点.results}}\`(数组) |
| \`url_reader\` | 读取网页 | "读取这个链接"、给了具体URL | \`url\`(完整URL) | \`maxLength\`(Integer, 100-50000, 默认5000) | \`{{节点.content}}\`(字符串) |
| \`calculator\` | 数学计算 | 计算表达式 | \`expression\` | - | \`{{节点.result}}\` (数值) |
| \`datetime\` | 时间操作 | 获取/计算日期时间 | \`operation\` (必填) | 见下方 datetime 配置 | \`{{节点.formatted}}\` |

> ⚠️ **datetime 工具配置**
> - \`operation\`: \`"now"\` | \`"format"\` | \`"diff"\` | \`"add"\` (必填)
> - \`unit\` 枚举值: \`"year"\` | \`"month"\` | \`"day"\` | \`"hour"\` | \`"minute"\` | \`"second"\`
> - \`amount\` 必须是**数字**类型
>
> **🎯 场景选择 (优先匹配)**:
> | 用户需求关键词 | operation | 示例配置 |
> |---------------|-----------|---------|
> | "当前时间"/"现在"/"今天"/"此刻" | \`now\` | \`{"operation": "now"}\` |
> | "X天后"/"加X小时"/"下个月" | \`add\` | \`{"operation": "add", "amount": 7, "unit": "day"}\` |
> | "相差"/"间隔"/"距离X天" | \`diff\` | \`{"operation": "diff", "date": "...", "targetDate": "..."}\` |
> | "格式化"/"转换格式" | \`format\` | \`{"operation": "format", "date": "...", "format": "YYYY年MM月DD日"}\` |
>
> 💡 **默认选 \`now\`**: 若需求仅涉及"时间"但无明确加减/差值/格式化，一律用 \`now\`
| \`code_interpreter\` | Python执行 | 数据分析/生成图表 | \`code\` | \`outputFileName\`, \`inputFiles\` | \`{{节点.result}}\` |

> **⚠️ Code Interpreter 最佳实践**:
> 绝大多数情况下，\`code\` 参数不应硬编码。**必须**先连接一个 "Coder LLM"（负责写代码），然后在此节点引用 \`{{CoderNode.response}}\` 执行。

## 5. Branch 节点

### 5.1 参数
| 参数 | 类型 | 说明 |
|------|------|------|
| \`condition\` | string | 判断条件 (空则返回 true) |

### 5.2 支持的表达式格式

**基础条件**:
1. **字符串检查**: \`Node.field.includes('xx')\` / \`.startsWith('xx')\` / \`.endsWith('xx')\`
   - ✅ \`用户输入.user_input.includes('http')\`

2. **相等判断**: \`Node.field === value\` / \`Node.field !== value\`
   - ✅ \`表单.formData.type === '企业'\`

3. **数值比较**: \`Node.field > number\` (支持 \`>\`, \`<\`, \`>=\`, \`<=\`)
   - ✅ \`评分.score >= 60\`

**逻辑组合 (AND/OR)**:
| 运算符 | 语法 | 示例 |
|--------|------|------|
| AND | \`条件1 && 条件2\` | \`输入.age >= 18 && 输入.verified === true\` |
| OR | \`条件1 \\|\\| 条件2\` | \`输入.type === 'image' \\|\\| 输入.type === 'video'\` |

> 💡 **语法提示**: 运算符前后需要空格 (\` && \` 而非 \`&&\`)

> 🔵 **全局变量引用**: 条件表达式支持引用**任意已执行节点**的输出，不限于直连上游节点。

### 5.3 输出变量
- \`{{分支.condition}}\`: 原始条件表达式
- \`{{分支.conditionResult}}\`: 判断结果 (true/false)
- \`{{分支.xxx}}\`: 透传上游节点字段 (如 \`response\`, \`result\` 等)

## 6. Output 节点
工作流的最终出口。

### 6.0 配置概览
| 模式 | sources 要求 | 适用场景 | 其他字段 |
|------|-------------|---------|---------|
| \`direct\` | 长度 = 1 | 单 LLM 直出 (最常用) | \`attachments\` 可选 |
| \`select\` | 长度 > 1 | Branch 分支 (输出首个非空值) | \`attachments\` 可选 |
| \`merge\` | 长度 > 1 | 多源内容拼接 (双换行分隔) | \`attachments\` 可选 |
| \`template\` | 不需要 sources | 格式化报告 (自定义模板) | 需配置 \`template\` 字段 |

> 🔴 **merge vs template 铁律**
> - **merge**: sources.value 必须是**纯变量** (如 \`{{A.response}}\`)，❌ 禁止混入静态文本
> - **template**: 用 \`template\` 字段写带格式的模板 (如 \`## 标题\\n{{A.response}}\`)

### 6.1 配置示例
\`\`\`json
// merge: 纯变量拼接
{"mode": "merge", "sources": [{"type": "variable", "value": "{{摘要.response}}"}, {"type": "variable", "value": "{{详情.response}}"}]}

// template: 自定义格式
{"mode": "template", "template": "## 摘要\\n{{摘要.response}}\\n\\n## 详情\\n{{详情.response}}"}
\`\`\`

## 7. ImageGen 节点 (图片生成)

### 7.0 参数表
| 参数 | 类型 | 默认值 | 取值范围/说明 |
|------|------|-------|-------------|
| \`model\` | string | \`Kwai-Kolors/Kolors\` | 见下方可用模型列表 |
| \`prompt\` | string | \`""\` | 图片描述，支持 \`{{变量}}\` |
| \`negativePrompt\` | string | \`""\` | 反向提示词 (仅 SD 模型支持) |
| \`imageSize\` | string | \`1024x1024\` | 图片比例 |

> 🔴 **ImageGen 使用铁律**
> - 涉及 **生成图片/创作图片/画图/生成海报** → 使用 ImageGen 节点
> - prompt 必须**明确描述**想要生成的图片内容
> - 支持从上游节点引用变量，如 \`{{LLM.response}}\`
> - 输出: \`{{节点.imageUrl}}\` (图片URL)

### 7.1 可用模型列表
| model 值 | 说明 |
|---------|------|
| \`Kwai-Kolors/Kolors\` | 可灵 - 默认，中文理解佳 |
| \`Qwen/Qwen-Image\` | 千问-文生图 |
| \`Qwen/Qwen-Image-Edit-2509\` | 千问-图生图 |

### 7.2 图片比例选项
| imageSize | 说明 |
|-----------|------|
| \`1024x1024\` | 1:1 正方形 (默认) |
| \`512x1024\` | 1:2 竖版 |
| \`1024x512\` | 2:1 横版 |

### 7.3 配置示例
\`\`\`json
{"id": "imagegen_1", "type": "imagegen", "data": {
  "label": "生成封面",
  "model": "Kwai-Kolors/Kolors",
  "prompt": "{{创意描述.response}}",
  "imageSize": "1024x1024"
}}
\`\`\`
`;
