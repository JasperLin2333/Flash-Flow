export const EFFICIENCY_RULES = `
# 🚀 效率与成本原则

1.  **避免冗余串联**: 除非逻辑上严格依赖，否则禁止将多个 LLM 节点串联（如 A -> B -> C）。
    *   ❌ 错误: Input -> LLM(大纲) -> LLM(第一章) -> LLM(第二章) ... (慢且贵)
    *   ✅ 正确: Input 同时连接多个 LLM 节点 (系统自动并行执行无依赖的节点)
2.  **并行优先**: 从同一节点连接多个下游节点即可并行执行（无需 Branch），Branch 仅用于条件分支（if-else）。
3.  **成本敏感**: "如无必要，勿增实体"。如果一个 LLM 节点能解决，不要拆成两个。
4.  **反馈速度**: 尽早让用户看到结果。对于复杂任务，优先输出概览，避免让用户长时间等待。
`;
